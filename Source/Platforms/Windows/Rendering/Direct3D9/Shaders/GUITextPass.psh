sampler2D font: register(s0);

struct PS_INPUT
{
        float4 Color: TEXCOORD0;
        float2 textureCoords: TEXCOORD1;
};

float4 main(PS_INPUT In): COLOR0
{
        return In.Color * tex2D(font, In.textureCoords).xxxx;
}
